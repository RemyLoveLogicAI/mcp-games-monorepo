{
  "name": "@mcp-games/kanban-harness",
  "version": "0.1.0",
  "description": "Kanban task harness — tick dispatcher, state machine, and HTTP control plane on :8794",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "tsx src/main.ts",
    "dev": "tsx watch src/main.ts",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
